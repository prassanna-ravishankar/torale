# Pull request checks
# Validates and builds on PR, but doesn't deploy

name: PR Checks

on:
  pull_request:
    branches:
      - main

jobs:
  # Validate project structure
  validate:
    name: Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate project structure
        run: |
          echo "🔍 Validating project structure..."
          test -f backend/Dockerfile || (echo "❌ backend/Dockerfile not found" && exit 1)
          test -f frontend/Dockerfile || (echo "❌ frontend/Dockerfile not found" && exit 1)
          test -f helmfile.yaml || (echo "❌ helmfile.yaml not found" && exit 1)
          test -d helm/torale || (echo "❌ helm/torale not found" && exit 1)
          echo "✅ All required files present"

  # Build and scan (reusable workflow)
  build:
    name: Build & Scan
    needs: validate
    uses: ./.github/workflows/build.yml
    with:
      push_latest: false
    secrets:
      GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}

  # Summary
  summary:
    name: PR Check Summary
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Summary
        run: |
          echo "✅ All checks passed!"
          echo "Images built and scanned successfully"
          echo "Ready to merge"

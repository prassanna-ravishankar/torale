# Fixture: deliberately drifted values. PostHog apiKey differs from the
# canonical workflow, so parity must FAIL when this file is compared
# against workflow-canonical.yml.
domains:
  legacy:
    frontend: torale.ai
    api: api.torale.ai
  live:
    frontend: webwhen.ai
    api: api.webwhen.ai

clerk:
  publishableKey: pk_live_Y2xlcmsud2Vid2hlbi5haSQ

posthog:
  enabled: true
  apiKey: "phc_SOMETHING_ELSE_ENTIRELY"
  apiHost: "https://eu.posthog.com"
